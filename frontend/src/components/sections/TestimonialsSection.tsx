"use client";

// Re-export the testimonials section from the modular structure
// This maintains backward compatibility while using the new modular structure internally
import TestimonialsSectionRouter from "./testimonials";

export default TestimonialsSectionRouter;
