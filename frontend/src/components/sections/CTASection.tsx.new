"use client";

// Re-export the CTA section from the modular structure
// This maintains backward compatibility while using the new modular structure internally
import CtaSectionRouter from "./cta";

export default CtaSectionRouter;
