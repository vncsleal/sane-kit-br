"use client";

// Re-export the Blog section from the modular structure
// This maintains backward compatibility while using the new modular structure internally
import BlogSectionRouter from "./blog";

export default BlogSectionRouter;
