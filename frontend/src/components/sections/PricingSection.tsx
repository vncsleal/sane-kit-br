"use client";

// Re-export the pricing section from the modular structure
// This maintains backward compatibility while using the new modular structure internally
import PricingSectionRouter from "./pricing";

export default PricingSectionRouter;
