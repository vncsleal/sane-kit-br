"use client";

// Re-export the CompareFeatures section from the modular structure
// This maintains backward compatibility while using the new modular structure internally
import CompareFeaturesRouter from "./compareFeatures";

export default CompareFeaturesRouter;
