"use client";

// Re-export the Newsletter section from the modular structure
// This maintains backward compatibility while using the new modular structure internally
import NewsletterSectionRouter from "./newsletter";

export default NewsletterSectionRouter;
