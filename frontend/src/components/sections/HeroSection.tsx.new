"use client";

// Re-export the Hero section from the modular structure
// This maintains backward compatibility while using the new modular structure internally
import HeroSectionRouter from "./hero";

export default HeroSectionRouter;
