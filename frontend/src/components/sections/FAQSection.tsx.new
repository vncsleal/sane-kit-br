"use client";

// Re-export the FAQ section from the modular structure
// This maintains backward compatibility while using the new modular structure internally
import FaqSectionRouter from "./faq";

export default FaqSectionRouter;
